# Copy to .env (git-ignored) and fill in real values for local runs / deployment.
# Nothing here is a real secret checked into the repo.
POSTGRES_PASSWORD=change-me
GRAFANA_ADMIN_PASSWORD=change-me

# Optional: healthcheck.py posts here on any service-down detection. Leave unset
# to just log loudly instead of sending a real webhook.
WEBHOOK_URL=
