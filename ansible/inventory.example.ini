# Copy to inventory.ini (git-ignored) and fill in the real host - typically the
# `instance_public_ip` output from `terraform output` after a real apply.
[swarmops]
swarmops-app ansible_host=203.0.113.10 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/swarmops-key.pem
